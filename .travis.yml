language: go
go:
  - "1.9.x"
  - "1.10.x"
install: make install-ci
env:
 # Set higher timeouts for Travis
 - TEST_TIMEOUT_SCALE=20 PACKAGE=github.com/m3db/m3metrics
sudo: required
dist: trusty
script:
 - make all
